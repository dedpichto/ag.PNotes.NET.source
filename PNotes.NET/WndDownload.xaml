<Window x:Class="PNotes.NET.WndDownload"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:styles="clr-namespace:PNotes.NET.styles"
        Title="Download" Height="300" Width="300"
        x:Name="DlgDownload"
        Style="{DynamicResource CustomWindowStyle}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        SizeToContent="Height"
        Topmost="True"
        styles:CustomWindowStyle.WindowBorder="MessageBox"
        TextElement.FontFamily="{Binding FontFamily}"
        TextElement.FontSize="{Binding FontSize}"
        TextElement.FontStretch="{Binding FontStretch}"
        TextElement.FontStyle="{Binding FontStyle}"
        TextElement.FontWeight="{Binding FontWeight}"
        Loaded="DlgDownload_Loaded"
        Unloaded="DlgDownload_Unloaded">
    <Grid>
        <TextBlock x:Name="lblDownload" Grid.Row="0" Margin="4" TextWrapping="Wrap"/>
    </Grid>
</Window>
